<p><%= @user.username %>'s page</p>
<p>Member since: <%= @user.created_at %></p>
<p>Featured items for sale!</p>
<p>Comment: Display 10 recent items, and provide a link to view all</p>
<p>Reviews</p>
<p><%= link_to "Show Reviews", 	user_reviews_path(params[:id]) %></p>
<% @user.reviews.each do |review| %>
	<p></p><%= review.rating %></p>
	<p><%= review.description %></p>
	<p><%= review.user.username %></p>
<% end %>
<p>Comment: Show 10 recent reviews, and provide a link to view all</p>
